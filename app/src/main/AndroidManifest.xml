<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jeet.notes_simple_kotlin">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_icons8_notes"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_icons8_notes"
        android:supportsRtl="true"
        android:theme="@style/Theme.Notes_Simple_Kotlin">
        <activity
            android:exported="true"
            android:name=".SplashScreen"
            android:theme="@style/Theme.Design.NoActionBar.fs" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>

        <activity
            android:name=".AddEditNoteActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
           >
        </activity>
    </application>

</manifest>